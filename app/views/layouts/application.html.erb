<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Involv</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="top-bar">
      <ul class="title-area">
        <li class="name"> 
          <h1> <%= link_to "INVOLV", issues_path %> </h1> 
        </li> 
      <ul>
      </nav>

    <section class="top-bar-section">
        <ul class="right">
          <% if current_user %>
              <li><%= link_to "Logged in as #{current_user.email}", "users/#{current_user.id}" %></li>
              <li class="divider"></li>
              <li><%= link_to "Log Out", logout_path %></li>
            <% else %>
              <li><%= link_to "Sign Up", signup_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Log In", login_path %></li>
          <% end %>
        </ul>
    </section>

    <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>